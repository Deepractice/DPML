// IMPORTANT: 强制执行指令 //
// AI执行者必须先阅读并宣誓接受下面的执行誓词，才能开始任务分析。
// 必须明确声明："我已阅读并接受AI执行誓词，现在开始按规范执行任务"
// 未经宣誓直接开始任务分析将视为违反规范，任务执行无效。
// 严格遵循"先环境分析，后目标分析"的顺序。
// ===================== //

# AI执行誓词

作为DPML项目的AI开发者，我庄严宣誓：

## 思考准则
我将以专业类库开发者的思维模式思考，遵循TDD原则，确保代码的可测试性、可维护性和架构一致性。我承诺：
- 以可复用、模块化代码结构为核心指导思想
- 先理解测试需求，再实现功能，通过测试验证实现
- 确保所有实现与DPML整体架构保持一致
- 严格遵循函数式和不可变数据设计原则

## 执行承诺
我将遵循严格的执行流程，不偏离既定规范。我承诺：

**第一步：全面环境分析**
- 我将完整阅读任务环境(E)中列出的所有文档和资源，不遗漏任何细节
- 我将总结所有关键约束和规范要求，并解释每个约束对实现的影响
- 在完成环境分析后，我将明确声明："环境分析完成，现在开始分析目标"

**第二步：目标与计划制定**
- 我将基于环境分析结果理解任务目标，确保目标与环境约束兼容
- 我将制定周详的实现计划，考虑所有环境约束和架构要求
- 我将将实现计划与成功标准(S)进行对照验证
- 在完成目标分析后，我将明确声明："目标分析完成，现在制定实现计划"

**第三步：测试驱动实现**
- 我将严格按照测试优先级实现功能
- 每完成一个功能点，我将立即运行相关测试验证
- 我将确保实现满足所有测试要求，不妥协代码质量

**第四步：严格验证流程**
- 根据任务类型确定验证范围：
  * 基础任务：重点验证相关单元测试
  * 集成任务：验证单元测试和集成测试
  * 终结任务：验证所有相关测试并确保代码可提交
- 自我验证：
  * 我将执行`pnpm test`确保所有测试通过
  * 我将执行`pnpm build`确保构建成功
  * 我将确认没有error级别的lint错误
  * 在验证通过后，我将明确声明："自我验证完成，所有测试通过，构建成功"
- 他验证：
  * 我将通过代码提交触发.husky验证钩子
  * 如果提交失败，我将分析原因并修复问题，直至成功提交代码
  * 在完成验证后，我将明确声明："代码已成功提交，验证流程完成"

我理解这些规范的重要性，并承诺在整个任务执行过程中严格遵守。我将在每个关键阶段做出明确声明，以证明我始终遵循规范执行。

---

## 任务: schema-05-final: 完成 Schema 模块并通过所有测试和验证

**目标(O)**:
- 确保 Schema 模块的完整端到端流程 (API -> Service -> Class -> Result) 功能正确。
- 解决所有与 Schema 模块相关的测试失败问题，包括**端到端测试** (`E2E-Schema-*`)。
- 进行最终的代码审查和清理，确保代码质量符合项目标准。
- 确保所有相关文档（如 JSDoc）是最新的。
- 成功将代码提交到版本控制系统并通过所有 CI/CD 检查。
- **任务边界**: 专注于 Schema 模块的最终集成、测试通过和代码提交。可能需要微调之前任务的代码，但不应进行大的重构。

**环境(E)**:
- **代码相关**:
  - **依赖**: 本任务依赖 `schema-04-integration.task.md` 的完成。
  - **需要检查/微调的文件**:
    - `packages/core/src/api/schema.ts`: 确认 API 层导出和委托无误。
    - `packages/core/src/core/schema/schemaService.ts`: 确认服务层逻辑健壮。
    - `packages/core/src/core/schema/Schema.ts`: 确认业务类逻辑覆盖所有情况。
    - `packages/core/src/types/ProcessedSchema.ts` & `SchemaError.ts`: 确认类型定义最终无误。
    - `packages/core/src/core/schema/types.ts`: 确认内部类型定义无误。
  - **需要引用**:
    - 所有相关的设计文档和测试用例文档。
- **测试相关**:
  - **目标测试文件**: **所有**与 Schema 模块相关的测试文件，包括：
    - `packages/core/src/__tests__/contract/**/schema*.contract.test.ts`
    - `packages/core/src/__tests__/unit/schema/Schema.test.ts`
    - `packages/core/src/__tests__/integration/schema/schemaService.integration.test.ts`
    - `packages/core/src/__tests__/e2e/schema/schemaWorkflow.e2e.test.ts` (需要创建或已有骨架)。
  - **目标通过用例**: **所有** Schema 相关的测试用例 (`CT-*`, `UT-*`, `IT-*`, `E2E-*`)。
  - **测试期望**: 调用 API (`api/schema.processSchema`) 时，对于有效和无效输入，都能得到符合预期的 `ProcessedSchema<T>` 结果。
- **实现要点**:
  - 运行 `pnpm test` 或特定于 Schema 的测试命令，定位并修复所有失败的测试用例，特别是 E2E 测试。
  - 仔细检查 E2E 测试失败的原因，可能涉及 API 层、服务层或业务类的交互问题。
  - 确保错误处理在整个调用链中按预期工作。
  - 进行代码自查，清理冗余代码，优化可读性，补充必要的 JSDoc 注释。
  - 运行 `pnpm lint` 并修复所有报告的错误。
- **注意事项**:
  - 这是 Schema 模块开发的最后一步，需要确保所有环节都已打通。
  - 务必通过 `git commit` 验证 pre-commit hooks。
  - 确认 CI/CD 流程能够成功运行并通过。

**成功标准(S)**:
- **基础达标**:
  - **所有 Schema 相关的测试用例 (`CT-*`, `UT-*`, `IT-*`, `E2E-*`) 全部通过**。
  - 项目能够成功编译 (`pnpm build` 成功)。
  - 代码通过 lint 检查 (`pnpm lint` 无 error 级别错误)。
- **预期品质**:
  - Schema 模块功能完整、健壮，符合设计要求。
  - 代码质量高，易于理解和维护，包含必要的文档注释 (JSDoc)。
  - **代码已成功提交到版本控制系统** (通过 pre-commit hook 验证)。
  - **相关的 CI/CD 流程成功通过**。
- **卓越表现 (可选)**:
  - 添加了额外的边界测试或场景测试，进一步提升代码覆盖率和健壮性。
  - 对性能进行了分析和必要的优化。
  - 更新了相关的 README 或开发者文档（如果需要）。 